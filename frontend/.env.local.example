# =============================================================================
# LOCAL DEVELOPMENT ENVIRONMENT CONFIGURATION
# =============================================================================
# Copy this file to .env.local for local development
# DO NOT use these settings in production!

# API Configuration (local backend)
NEXT_PUBLIC_API_URL=http://localhost:8000/api

# Site Configuration (not needed for local dev)
# NEXT_PUBLIC_SITE_URL=http://localhost:3000
# NEXT_PUBLIC_DOMAIN=localhost

# Google Maps API Key (optional - for development/testing)
# NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=your_dev_google_maps_api_key

# Development Notes:
# - The backend should be running on localhost:8000
# - Frontend will run on localhost:3000 by default
# - NEXT_PUBLIC_ prefix makes variables available in the browser
# - Never put sensitive secrets in NEXT_PUBLIC_ variables
